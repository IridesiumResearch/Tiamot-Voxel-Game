# A domain with hills in it, so the horizon has something to show.
# A TEST FIXTURE, not content — see docs/fixtures/README.md.
id = "relief"
name = "Relief"
version = "0.1.0"
depends = ["core >=0.1"]
description = "One domain of noise terrain, for looking at chunk LOD. Copy into game/, delete when done."
license = "GPL-3.0-only"
